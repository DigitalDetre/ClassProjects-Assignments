To compile the java file do
javac SIM.java

To run the class file do
java SIM <M> <N> <tracefile>

Example:
java SIM 4 1 gobmk_trace.txt

Make sure the trace files are in the same directory as the executable